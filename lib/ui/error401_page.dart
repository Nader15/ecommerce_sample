import 'package:flutter/material.dart';

class Error401Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("Error 401"),));
  }
}
